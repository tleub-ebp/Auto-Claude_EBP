{
	"episode_id": "test-runner/rust-flaky-timeout",
	"agent_id": "test-runner",
	"language": "rust",
	"workflow": "feature-build",
	"task": "An integration test times out on CI roughly one run in five, and passes locally every time.",
	"baseline_signals": [],
	"context": {
		"why_this_is_here": "The archived baseline failed this one. It is in the corpus so a candidate that fixes it is measured as an improvement rather than assumed to be one.",
		"observable": "CI run history, 3 failures in 15 runs"
	}
}
