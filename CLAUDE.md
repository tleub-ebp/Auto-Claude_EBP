# CLAUDE.md

Claude Code loads this file automatically. Everything below is a pointer — the content
lives in files every other agent can read too, so there is one set of rules, not two.

@AGENTS.md

@docs/CLAUDE.md

## Precedence

`docs/CLAUDE.md` is normative. `AGENTS.md` and the per-directory `AGENTS.md` indexes are
navigation aids: they say where to look, not what is allowed. Where they disagree,
`docs/CLAUDE.md` wins and the index is the thing to fix.
