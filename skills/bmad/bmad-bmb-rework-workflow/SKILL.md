---
name: bmad-bmb-rework-workflow
description: Rework a Workflow to a V6 Compliant Version
metadata:
  workpilot:
    # Not emitted until the BMAD runtime is present. These wrappers are
    # pointers into _bmad/, which is generated by BMAD's own installer
    # and gitignored -- so on a fresh clone they resolved to nothing and
    # every one of them failed on invocation.
    requires: { runtime: "_bmad/bmb/workflows/workflow/workflow-rework-workflow.md" }
---

IT IS CRITICAL THAT YOU FOLLOW THIS COMMAND: LOAD the FULL @_bmad/bmb/workflows/workflow/workflow-rework-workflow.md, READ its entire contents and follow its directions exactly!
