---
name: bmad-bmb-edit-workflow
description: Edit existing BMAD workflows while maintaining integrity
metadata:
  workpilot:
    # Not emitted until the BMAD runtime is present. These wrappers are
    # pointers into _bmad/, which is generated by BMAD's own installer
    # and gitignored -- so on a fresh clone they resolved to nothing and
    # every one of them failed on invocation.
    requires: { runtime: "_bmad/bmb/workflows/workflow/workflow-edit-workflow.md" }
---

IT IS CRITICAL THAT YOU FOLLOW THIS COMMAND: LOAD the FULL @_bmad/bmb/workflows/workflow/workflow-edit-workflow.md, READ its entire contents and follow its directions exactly!
