---
name: bmad-editorial-review-structure
description: Execute editorial-review-structure
metadata:
  workpilot:
    # Not emitted until the BMAD runtime is present. These wrappers are
    # pointers into _bmad/, which is generated by BMAD's own installer
    # and gitignored -- so on a fresh clone they resolved to nothing and
    # every one of them failed on invocation.
    requires: { runtime: "_bmad/core/tasks/editorial-review-structure.xml" }
---

# editorial-review-structure

Read the entire task file at: {project-root}/_bmad/core/tasks/editorial-review-structure.xml

Follow all instructions in the task file exactly as written.
