---
name: bmad-tea-teach-me-testing
description: Teach testing progressively through structured sessions. Use when user says "lets learn testing" or "I want to study test practices"
metadata:
  workpilot:
    # Not emitted until the BMAD runtime is present. These wrappers are
    # pointers into _bmad/, which is generated by BMAD's own installer
    # and gitignored -- so on a fresh clone they resolved to nothing and
    # every one of them failed on invocation.
    requires: { runtime: "_bmad/tea/workflows/testarch/teach-me-testing/workflow.md" }
---

IT IS CRITICAL THAT YOU FOLLOW THIS COMMAND: LOAD the FULL @_bmad/tea/workflows/testarch/teach-me-testing/workflow.md, READ its entire contents and follow its directions exactly!
